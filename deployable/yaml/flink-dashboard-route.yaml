apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: flink-dashboard
  namespace: confluent-platform
spec:
  to:
    kind: Service
    name: basic-flink-deployment-rest
  port:
    targetPort: 8081
  tls:
    termination: edge
  wildcardPolicy: None

